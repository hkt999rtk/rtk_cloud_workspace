# Non-secret default description for the 100K Home IoT Device Shadow load test.
# Keep LINODE_TOKEN and other secrets in ~/.env, not in this file.

HOME100K_ENV_ROOT=cloud_env/staging/lke
HOME100K_BRANDNAME=RTK
HOME100K_REGION=us-sea
HOME100K_VM_LABEL_PREFIX=lg

HOME100K_REMOTE_WORKSPACE=/root/rtk_cloud_workspace
HOME100K_REMOTE_ENV_ROOT=/root/rtk_cloud_workspace/cloud_env/staging/lke
HOME100K_REMOTE_OUT_ROOT=/var/lib/home-100k
HOME100K_SSH_USER=root
HOME100K_SSH_KEY=~/.ssh/id_ed25519_rtkcloud
HOME100K_AUTHORIZED_KEY_FILE=~/.ssh/id_ed25519_rtkcloud.pub

HOME100K_STATUS_INTERVAL_SECONDS=30
HOME100K_STAGE_WARM_UP=30s
HOME100K_STAGE_STEADY=90s
HOME100K_STAGE_COOL_DOWN=30s
# Canonical load size for this run profile. The home-100k directory and VM
# label prefix are package names only; change the target device/user count here.
HOME100K_DEVICES=9000
# Optional HOME100K_USERS overrides the derived app-user count. When omitted,
# planner derives users as ceil(HOME100K_DEVICES / HOME100K_DEVICES_PER_USER).
HOME100K_DEVICES_PER_USER=20
HOME100K_RUNNER_MODE=live
# Remote runner daemon file-descriptor headroom for MQTT sockets. This is not a
# target connection count; the actual device/user count is controlled above.
# If the generator hits "too many open files", the run is INCOMPLETE because
# the generator saturated before the server target was measured.
HOME100K_RUNNER_NOFILE_LIMIT=1048576
HOME100K_COMMAND_CONCURRENCY=100
HOME100K_SHADOW_COMMAND_TIMEOUT=30s
HOME100K_COORDINATOR_START_DELAY_MS=3000
HOME100K_CREDENTIAL_BUNDLE_FORMAT=sqlite-gzip
# Live device sessions subscribe once after MQTT connect and keep reading shadow
# deltas for the whole online lifetime through bounded Go reader goroutines.
HOME100K_DEVICE_SESSION_MODEL=lifetime-subscription
HOME100K_FUNCTIONAL_SUCCESS_THRESHOLD_PERCENT=99.5
HOME100K_CLIENT_TARGET_COMPLETENESS_PERCENT=100
HOME100K_EXACT_EVENT_CORRELATION_PERCENT=100
HOME100K_AGGREGATE_CORRELATION_TOLERANCE_PERCENT=0.1
HOME100K_AGGREGATE_CORRELATION_MIN_TOLERANCE=5
HOME100K_MQTT_ADDR=auto-public-mqtt
HOME100K_MQTT_PUBLIC_LB_COUNT=1
HOME100K_VIDEO_CLOUD_PUBLIC_BASE_URL=https://video-cloud-staging.realtekconnect.com
HOME100K_VIDEO_CLOUD_TOKEN_BASE_URL=https://device.video-cloud-staging.realtekconnect.com
HOME100K_ACCOUNT_MANAGER_BASE_URL=https://account-manager.video-cloud-staging.realtekconnect.com
# Optional: path to a logger env file containing CLOUD_LOGGER_ENDPOINT and
# CLOUD_LOGGER_INGEST_TOKEN. Keep the token in the referenced env file, not here.
HOME100K_CLOUD_LOGGER_ENV=
HOME100K_NODE_RESOURCE_STATUS=1
HOME100K_K8S_NODE_RESOURCE_STATUS=1
